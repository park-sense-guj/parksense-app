#pragma once

// Copy this file to secrets.h (gitignored) before flashing.
#define PARKSENSE_WIFI_SSID "YOUR_WIFI_SSID"
#define PARKSENSE_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Optional. Realtime Database secret, or a Firebase ID token.
// If empty, the node tries anonymous Auth with PARKSENSE_FIREBASE_API_KEY.
#define PARKSENSE_FIREBASE_AUTH ""

// Firebase web API key (same as EXPO_PUBLIC_FIREBASE_API_KEY).
#define PARKSENSE_FIREBASE_API_KEY ""
