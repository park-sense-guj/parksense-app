{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'admin'",
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".validate": "newData.hasChildren(['userId', 'fullName', 'email', 'role', 'registeredOn']) && newData.child('userId').val() === $uid",
        "role": {
          ".validate": "newData.isString() && ((!data.exists() && (newData.val() === 'user' || (newData.val() === 'admin' && newData.parent().child('email').val() === 'admin@parksense.app'))) || (data.exists() && newData.val() === data.val()) || (data.exists() && auth.uid === $uid && newData.val() === 'admin' && newData.parent().child('email').val() === 'admin@parksense.app') || root.child('users').child(auth.uid).child('role').val() === 'admin')"
        },
        "email": {
          ".validate": "newData.isString() && newData.val().length > 3"
        },
        "fullName": {
          ".validate": "newData.isString() && newData.val().length > 0"
        },
        "preferredLocation": {
          ".validate": "newData.isString() || !newData.exists()"
        }
      }
    },

    "parkingSlots": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$slotId": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['slotNumber', 'locationName', 'latitude', 'longitude', 'status']) && (newData.child('status').val() === 'Available' || newData.child('status').val() === 'Occupied')",
        "heldByUserId": {
          ".validate": "(!newData.exists() && (!data.exists() || data.val() === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin')) || (newData.exists() && newData.isString() && (newData.val() === data.val() || root.child('users').child(auth.uid).child('role').val() === 'admin' || (newData.val() === auth.uid && (!data.exists() || data.val() === auth.uid || !root.child('parkingSlots').child($slotId).child('heldUntil').exists() || root.child('parkingSlots').child($slotId).child('heldUntil').val() < now))))"
        },
        "occupiedByUserId": {
          ".validate": "(!newData.exists() && (!data.exists() || data.val() === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin')) || (newData.exists() && newData.isString() && (newData.val() === data.val() || newData.val() === auth.uid))"
        }
      }
    },

    "sensors": {
      ".read": "auth != null",
      ".write": "auth != null && (root.child('users').child(auth.uid).child('role').val() === 'admin' || (newData.exists() && newData.child('sensorStatus').val() !== 'Faulty'))"
    },

    "parkingHistory": {
      "$userId": {
        ".read": "auth != null && (auth.uid === $userId || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $userId || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        "$historyId": {
          ".validate": "newData.hasChildren(['userId', 'slotId', 'slotNumber', 'locationName', 'entryTime', 'bookingDate']) && newData.child('userId').val() === $userId"
        }
      }
    },

    "notifications": {
      "$userId": {
        ".read": "auth != null && (auth.uid === $userId || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        ".write": "auth != null && (auth.uid === $userId || root.child('users').child(auth.uid).child('role').val() === 'admin')",
        "$notificationId": {
          ".validate": "newData.hasChildren(['userId', 'message', 'createdTime', 'isRead']) && newData.child('userId').val() === $userId"
        }
      }
    },

    "lotWatchers": {
      ".read": "auth != null",
      ".write": "auth != null"
    },

    "driverPresence": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
